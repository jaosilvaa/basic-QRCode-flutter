import 'package:flutter/material.dart';

class AppColors{

  // Cores principais do tema
  static const Color primary = Color(0xFFBBFB4C); // Verde neon principal
  static const Color secondary = Color(0xFF18191B); // Preto secundário
  static const Color background = Color(0xFF000000); // Preto principal
  static const Color surface = Color(0xFF2D2D2D); // Cinza escuro para bordas
  static const Color error = Color(0xFFFF5252); // Vermelho para erros

  //Cores de Texto
  static const Color textPrimary = Color(0xFFFFFFFF); // Branco principal
  static const Color textSecondary = Color(0xFF797A7D); // Cinza claro
  static const Color textHint = Color(0xFF9E9E9E); 

  // Cores de superfície
  static const Color cardBackground = Color(0xFF000000); // Fundo dos cards
  static const Color inputBackground = Color(0xFF000000); // Fundo dos inputs
  static const Color drawerBackground = Color(0xFF000000); // Fundo do drawer

  // Cores específicas para elementos
  static const Color borderColor = Color(0xFF2D2D2D); // Cor das bordas
  static const Color borderFocused = Color(0xFFBBFB4C); // Borda quando focado
  static const Color dividerColor = Color(0xFF2D2D2D); // Cor dos divisores

  // Cores para o tema light (futuro)
  static const Color lightBackground = Color(0xFFFFFFFF);
  static const Color lightSurface = Color(0xFFF5F5F5);
  static const Color lightTextPrimary = Color(0xFF000000);
  static const Color lightTextSecondary = Color(0xFF666666);
  static const Color lightBorderColor = Color(0xFFE0E0E0);

  // Cores de status
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);

  //Favorite button
  static const Color favoriteColor = Colors.red;

  // Transparências
  static const Color white12 = Color(0x1FFFFFFF);
  static const Color white30 = Color(0x4DFFFFFF);
  static const Color white70 = Color(0xB3FFFFFF);
  static const Color black12 = Color(0x1F000000);
  static const Color black26 = Color(0x42000000);
  static const Color black54 = Color(0x8A000000);
  static const Color black87 = Color(0xDD000000);

  //Cores createapp
  static const Color iconBackgroundDark = Color(0xFFFFFAFF);

  // Cores Neutras
  static const Color black = Colors.black;
  static const Color white = Colors.white;
  static const Color grey = Colors.grey;

}